description: "Breaking GPT & Claude — workshop eval"

prompts:
  - file://prompts/system-helpful.txt
  - file://prompts/system-strict.txt

providers:
  - id: openai:gpt-4o-mini
    config:
      temperature: 0
      max_tokens: 400

  # Uncomment to run GPT vs Claude side-by-side.
  # Requires ANTHROPIC_API_KEY in .env.
  # - id: anthropic:messages:claude-haiku-4-5
  #   config:
  #     temperature: 0
  #     max_tokens: 400

tests:
  - file://tests/jailbreaks.yaml
  - file://tests/hallucinations.yaml
  - file://tests/cost-context.yaml

defaultTest:
  options:
    provider:
      id: openai:gpt-4o-mini
      config:
        temperature: 0
