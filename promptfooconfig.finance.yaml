description: "Breaking GPT & Claude — workshop eval (FinanceBot brokerage assistant)"

prompts:
  - file://prompts/financebot.txt

providers:
  - id: openai:gpt-4o-mini
    config:
      temperature: 0
      max_tokens: 400

  # OPTIONAL — multi-model comparison. Uncomment and add OPENROUTER_API_KEY to .env.
  # Free-tier rate limit: 20 req/min, 200 req/day. The `:free` suffix is required.
  # - id: openrouter:meta-llama/llama-3.3-70b-instruct:free
  # - id: openrouter:google/gemma-4-31b-it:free
  # - id: openrouter:qwen/qwen3-next-80b-a3b-instruct:free

  # OPTIONAL — GPT vs Claude. Requires ANTHROPIC_API_KEY.
  # - id: anthropic:messages:claude-haiku-4-5

tests:
  - file://tests/finance-jailbreaks.yaml
  - file://tests/hallucinations.yaml
  - file://tests/cost-context.yaml
